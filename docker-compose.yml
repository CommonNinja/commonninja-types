version: "3.9"

services:
  swagger:
    image: redocly/redoc
    ports:
      - 8083:80      
    environment:
      - PAGE_TITLE=Integration Utils
      - SPEC_URL=swagger/openapi.yml
    volumes:
      - ./lib/swagger:/usr/share/nginx/html/swagger
